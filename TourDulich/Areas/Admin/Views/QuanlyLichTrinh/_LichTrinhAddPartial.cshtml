@model List<TourDulich.Models.LichTrinhTour>

<form id="formThemLichTrinh">
    <input type="hidden" name="ID_Tour" value="@Model.FirstOrDefault()?.ID_Tour" />
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th style="width: 100px; text-align: center;">Ngày Thứ</th>
                <th style="width: 200px;">Tiêu Đề</th>
                <th>Nội Dung</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Count; i++)
            {
                <tr>
                    <td style="text-align:center; vertical-align: middle;">
                        @Model[i].NgayThu
                        <input type="hidden" name="model[@i].NgayThu" value="@Model[i].NgayThu" />
                        <input type="hidden" name="model[@i].ID_Tour" value="@Model[i].ID_Tour" />
                    </td>
                    <td>
                        <textarea name="model[@i].TieuDe" class="form-control" rows="3"></textarea>
                    </td>
                    <td>
                        <textarea name="model[@i].NoiDung" class="form-control" rows="3"></textarea>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <button type="button" id="btnLuuThemLichTrinh" class="btn btn-primary">Lưu Lịch Trình</button>
</form>

